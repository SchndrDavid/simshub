# Host port the container is published on. Change it only if it clashes
# with another service on the same host.
SIMSHUB_PORT=8105

# UID/GID that owns ./data. Match them to the host user so the container
# can write the SQLite database.
SIMSHUB_UID=1000
SIMSHUB_GID=1000
